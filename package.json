{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "concurrently -n FRONTEND,CMS -c blue,green \"npm run dev --prefix frontend\" \"npm run dev --prefix tfpa-cms\""
  }
}
